1、并发
交替做不同事情的能力，所谓的高并发就是在一个时间段内由时间片管理交替执行任务，
如I/O多路复用技术，就是多个I/O操作复用一个线程，该线程不停的切换时间片

2、并行
就是一时间点做许多相同的或者不同的事情

3、同步
同步并不是字面上的意思同时或一起，而是协同步调、相互配合。是按照先后顺序执行的。
同步就是：在发出一个功能调用后，没有得到返回值之前一直等待，不会继续向下执行

4、异步
与同步刚好相反，就是在发出一个功能调用时，不管有没有得到结果，都继续执行下去。

总结来说，同步和异步的区别：请求发出后，是否需要等待结果，才能继续执行其他操作

5、